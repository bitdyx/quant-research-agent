# Windows Task Scheduler

Use the CLI as the scheduled target:

```powershell
D:\program\Anaconda\python.exe -m quant_research_agent run-daily
```

Recommended working directory:

```text
D:\python\worldquant\quant_research_agent
```

Recommended schedule:

- Daily
- One run per day after market close or early morning before your review session
